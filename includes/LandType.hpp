/*
 * LandType:hpp
 */

#ifndef LANDTYPE_HPP
#define LANDTYPE_HPP

enum LandType {
    PLAIN = (1<<0),
    OCEAN = (1<<1),
    COAST = (1<<2),
    SHORE = (1<<3),
    FOREST = (1<<4),
    MOUNTAIN = (1<<5),
    PEAK = (1<<6)
};

#endif /* LANDTYPE_HPP */
