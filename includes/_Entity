/*
 * Entity.hpp
 */

#ifndef ENTITY_HPP
#define ENTITY_HPP

#include "../includes/Game.hpp"
#include "../includes/TexManager.hpp"
#include "../includes/Vector2d.hpp"

class Entity {

public:
    Entity();
    Entity(const char *file);
    Entity(const char *file, Tile *t);
    ~Entity();

    void update();
    void render();

protected:
    SDL_Surface *m_SDLsurf;
    SDL_Texture *m_SDLtex;
    Tile *m_tile;
};

#endif /* ENTITY_HPP */
